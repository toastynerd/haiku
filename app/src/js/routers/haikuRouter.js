'use strict';

var Backbone            = require('backbone');
var Haiku               = require('../models/haiku');
var HaikuView           = require('../views/haikuView');
var HaikuCollectionView = require('../collectionViews/haikuCollectionView');

module.exporst = Backbone.Router.extend({
  
});
